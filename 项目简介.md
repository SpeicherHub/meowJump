# 项目简介

## 项目定位

`Meow Jump` 是一个面向手机浏览器的竖屏跳跃小游戏，核心玩法参考经典纵向平台跳跃游戏：主角自动弹跳，玩家通过按住屏幕左右两侧控制横向移动，踩平台持续向上，掉出屏幕后游戏结束。

项目的主角 IP 是一只橘白猫：圆脸、大头、粉色腮红、眨眼表情、浅蓝背心和蓝色短裤。当前视觉方向是可爱、明亮、手绘、粗描边、适合休闲传播的移动端小游戏。

## 当前技术实现

- 技术栈：`Vite + Canvas`
- 入口文件：
  - `index.html`
  - `src/main.js`
  - `src/styles.css`
- 运行方式：
  - `npm install`
  - `npm run build`
  - `npm run preview -- --host 0.0.0.0 --port 5175`
- 当前局域网试玩曾使用：
  - `http://172.168.10.77:5175`
- 当前公网试玩使用 GitHub Pages：
  - `https://speicherhub.github.io/meowJump/`
- GitHub 仓库：
  - `https://github.com/SpeicherHub/meowJump`

开发时遇到过 Windows 沙箱下 `vite dev` 模块解析触发 `spawn EPERM` 的情况，因此当前更推荐使用生产构建后的 `preview` 服务进行手机测试。

## 当前部署方案

当前线上方案是“代码放 GitHub，图片从外部资源托管读取”：

- GitHub Pages 负责托管构建后的 HTML、CSS、JS。
- GitHub Actions 工作流位于 `.github/workflows/deploy.yml`。
- 每次推送到 `main` 后，Actions 会执行 `npm install`、`npm run build`，然后发布 `dist`。
- 线上访问地址为 `https://speicherhub.github.io/meowJump/`。
- 运行时图片资源从外部只读资源域名加载，不在公开文档中暴露源站地址。

这样做的原因：

- 角色、背景、平台等 PNG 图片体积较大。
- 为了避开大图片上传，GitHub Pages 版只上传代码和构建配置。
- 图片继续使用已经配置好的外部资源托管地址。
- 仓库会更轻，GitHub Actions 部署更快，后续改代码也更省事。

需要注意：

- 资源托管侧只需要公共读，不要开启公共读写。
- `src/main.js` 中的 `assetBaseUrl` 指向远程资源根地址。
- `src/styles.css` 中开始面板背景图也引用远程资源地址。
- 如果以后改成把图片也放回 GitHub，需要同步修改这些资源路径。
- 如果远程资源读策略、域名或文件路径变化，GitHub Pages 页面会打开但图片会加载失败。

## 当前玩法

- 游戏状态：
  - `ready`：开始界面
  - `playing`：游戏中
  - `gameOver`：结算与重开
- 操作方式：
  - 手机端按住屏幕左半区向左移动
  - 按住屏幕右半区向右移动
  - 桌面端支持方向键和 `A/D`
- 横向边界：
  - 主角可以从屏幕一侧穿出并从另一侧出现
- 计分规则：
  - 统一使用米制显示
  - 向上每一格算 `1m`
  - 当前配置中 `meterGridHeight: 100`
- 最高分：
  - 使用 `localStorage` 保存

## 当前数值与手感

核心配置集中在 `src/main.js` 的 `config` 中。

当前主要参数：

- 虚拟游戏宽度：`390`
- 重力：`1720`
- 跳跃初速度：`-760`
- 横向移动速度：`430`
- 主角尺寸：`140 x 151`
- 普通平台尺寸：`128 x 34`
- 平台间距：`86 - 132`

为了适配不同全面屏手机，游戏使用固定虚拟宽度，视口高度按实际屏幕换算。这样不同机型只会看到更多或更少纵向区域，不会因为物理像素变化改变游戏难度。

用户手机规格曾提到为 `2376 x 1080` 物理像素，约对应常见的高屏占比 CSS 视口，需要保持：

- 角色不被横向压缩
- 平台尺寸稳定
- UI 不挡住刘海、状态栏、底部手势区
- 背景可裁切但中间游戏区域要干净

## 碰撞判定

早期碰撞判定偏宽，出现过“看起来没有踩到平台，但仍然弹起”的问题。

当前已改为脚底判定：

- 只检测猫咪底部中间一小段区域
- 平台左右边缘会内缩作为有效落点
- 脚底和平台必须有足够重叠才会反弹

相关参数：

- `footHitboxWidthRatio: 0.32`
- `minFootOverlapRatio: 0.35`
- `platformLandingInset: 12`

## 当前素材

素材位于 `public/assets/`。

本地开发时仍保留完整素材目录，便于预览和重新构建；当前 GitHub Pages 线上版不依赖 GitHub 仓库中的图片文件，而是从远程资源域名加载同名路径的图片。

角色素材：

- `character-idle.png`
- `character-jump.png`
- `character-fall.png`
- `character-rocket.png`

平台素材：

- `scenes/*-platform-normal.png`
- `scenes/*-platform-fragile.png`

背景素材：

- `scenes/*-background.png`

道具素材：

- `rocket-powerup.png`

同时保留了部分 `*-source.png` 文件，这些是 ImageGen 生成时的色键源文件，经过本地抠图后得到透明 PNG。

## 脆弱木板玩法

当前已加入第一层难度机制：脆弱木质平台。

规则：

- `0 - 1m`：不出现脆弱木板
- `1m - 20m`：脆弱木板出现概率 `10%`
- `20m - 50m`：脆弱木板出现概率 `20%`
- `50m - 100m`：脆弱木板出现概率 `25%`
- `100m+`：脆弱木板出现概率 `30%`
- 木板可以正常弹跳一次
- 被踩中后立即碎裂并从平台列表移除

反馈：

- 木板会分成 3 段碎片
- 碎片下坠、旋转并淡出
- 支持 `navigator.vibrate` 的手机会触发短震动
- 不支持震动时自动跳过，不影响游戏

相关参数：

- `fragilePlatformStartMeters: 1`
- `fragilePlatformMidMeters: 20`
- `fragilePlatformChance: 0.1`
- `fragilePlatformHardMeters: 50`
- `fragilePlatformMidChance: 0.2`
- `fragilePlatformHardChance: 0.25`
- `fragilePlatformExpertMeters: 100`
- `fragilePlatformExpertChance: 0.3`
- `fragileBreakDuration: 0.65`
- `fragileBreakGravity: 980`

## 火箭道具玩法

当前火箭道具会按高度区间生成，不使用随机概率。

规则：

- 从 `15m` 开始生成火箭
- 每 `20m` 一个区间最多生成 `1` 个火箭，例如 `15m - 34m`、`35m - 54m`、`55m - 74m`
- 每次场景切换前 `15m` 内不生成火箭，例如 `35m - 49m`、`85m - 99m`
- 火箭只会出现在普通平台上，不会出现在脆弱木板上
- 玩家碰到火箭后进入火箭模式，向上冲刺 `10m`
- 冲刺结束后回到普通模式，并保留一段向上的退出速度

相关参数：

- `rocketSpawnEveryMeters: 20`
- `rocketFirstSpawnMeters: 15`
- `rocketSceneTransitionBlockMeters: 15`
- `rocketBoostMeters: 10`
- `rocketBoostSpeed: 1200`
- `rocketExitVelocity: -520`

## UI 状态

顶部 HUD 包含：

- 暂停按钮
- 当前分数
- 最高分

这三个控件已统一高度，使用 CSS 变量：

- `--hud-height: 46px`

UI 需要继续保持：

- 安全区适配
- 不遮挡主角和平台
- 在全面屏手机上高度一致、对齐稳定

## 已确认的视觉方向

当前游戏主视觉是天空篮球场景：

- 明亮天空
- 云朵
- 浮空篮球场
- 篮筐、旗帜、树丛等装饰
- 普通平台为云端篮球场跳板
- 脆弱平台为裂缝木板

当前已经接入按高度变化的场景：

- `0 - 50m`：天空篮球
- `50 - 100m`：糖果物语
- `100 - 150m`：森林奇境
- `150 - 200m`：3D重庆
- `200m+`：浩瀚太空

场景切换效果：

- 背景会在切换点前 `5m` 开始渐变。
- 渐变时长为 `1000ms`。
- 平台会在切换点前 `10m` 使用下一场景的平台图，给玩家一点视觉预告。
- 火箭在每个场景切换前 `15m` 内不会刷新，避免切换前冲刺破坏场景过渡节奏。

用户后续对场景生成提出了两个重要要求：

- 背景元素尽量放在左右两侧，中间不要有太多东西，保证猫咪和跳板可见性
- 整体颜色不要太出挑，不要抢前景
- `3D重庆` 的跳板方向需要结合钢结构大桥元素，例如桥面、钢梁、拉索、桥拱

## 后续建议

优先级较高的后续工作：

1. 继续微调平台生成、难度曲线和道具/障碍物。
2. 复测 GitHub Pages 线上版本，确认场景渐变、火箭禁刷区、远程图片加载都正常。
3. 进一步优化五个场景素材，遵守“中间留空、两侧布景、低干扰色彩”的要求。
4. 为远程图片资源整理更稳定的发布流程，避免手动上传遗漏。
5. 后续如果改为自有域名或 CDN，需要同步更新文档中的线上地址和资源路径说明。

实现新玩法时，应优先保持：

- 手机端操作手感
- 前景可读性
- 角色 IP 露出
- 低学习成本
- 快速试玩体验
